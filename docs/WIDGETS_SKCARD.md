# SKCard

Foundational surface container — Compose `SKCard` / XML `SKCardView`.

```kotlin
SKCard(onClick = { /* optional */ }, appearance = SKAppearanceConfig.Card) {
    SKText(text = "Content")
}
```

Uses `SKAppearanceConfig.Card` (surface + elevation). Specialized Material card variants deferred.
