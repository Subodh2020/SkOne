# SKDivider

Decorative hairline separator — Compose `SKDivider` / XML `SKDividerView`.

```kotlin
SKDivider(orientation = SKDividerOrientation.Horizontal)
SKDivider(orientation = SKDividerOrientation.Vertical)
```

Hidden from TalkBack by default (`clearAndSetSemantics` / `IMPORTANT_FOR_ACCESSIBILITY_NO`).
