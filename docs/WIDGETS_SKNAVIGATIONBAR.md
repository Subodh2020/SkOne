# SKNavigationBar

Primary destinations — Compose `SKNavigationBar` / XML `SKNavigationBarView`.

Exclusive selection via `selectedId`. Items expose Tab role + selected state.

**Deferred:** badges, nested navigation graph integration, Material NavigationBar chrome parity.
