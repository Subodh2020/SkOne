# SKProgressIndicator

Determinate or indeterminate progress — Compose `SKProgressIndicator` / XML `SKProgressIndicatorView`.

```kotlin
SKProgressIndicator(
    progress = 0.4f,
    indeterminate = false,
    style = SKProgressStyle.Linear, // or Circular
)
```

Uses lightweight Compose/View animation — no custom animation framework.
