# SKSectionHeader

Lightweight section title — Compose `SKSectionHeader` / XML `SKSectionHeaderView`.

```kotlin
SKSectionHeader(
    title = "Today",
    supportingText = "Priority",
    actionLabel = "See all",
    onAction = { },
)
```

Exposed as an accessibility heading by default.
