# SKSnackbar

Transient feedback — Compose `SKSnackbar` / XML `SKSnackbarView`.

Host controls visibility. Optional action. Polite live region by default.

**Deferred:** auto-dismiss timer host helper, queueing overlay manager.
